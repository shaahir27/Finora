# Session 6 — Differentiators, Receipts, Reminders, Rate Limiting, Polish, Demo Rehearsal

This folder is populated by the coding agent while building this session, per `docs/BUILD_LOGGING_PROTOCOL.md`.

Expected contents once this session is complete:
- One `feature_[name].md` per distinct feature built this session, using `docs/templates/log_schema.md`.
- One `session_XX_summary.md`, written once every feature log above is in place.

This README is a placeholder only — it is not itself a feature log and should not be edited to contain feature content. It can be deleted once the folder has real content, or left in place; either is fine.
